package com.intexsoft.apifacadeservice.controllers.clientcontrollers.project;

import lombok.RequiredArgsConstructor;
import org.springframework.web.bind.annotation.*;

@RestController
@RequiredArgsConstructor
@RequestMapping("/api/projects") // todo check
public class ProjectEmployeesController {

    //router.get('/:id/projects',
    //router.post('/:id/projects',
    //router.delete('/:id/projects/:project_id',

}
